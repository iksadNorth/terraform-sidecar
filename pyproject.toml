[project]
name = "terraform-sidecar"
version = "0.1.0"
description = "Terraform sidecar FastAPI service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.5",
    "uvicorn>=0.30.1",
    "pydantic-settings>=2.6.1",
]
